import React from "react";
import { ActivityIndicator, View } from "react-native";

const LoadingData = () => {
  return (
    <View>
      <ActivityIndicator size="large" color="#FF3688" />
    </View>
  );
};

export default LoadingData;
